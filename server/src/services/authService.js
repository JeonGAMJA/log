// auth에 관한 로직처리(회원정보 조회,회원정보 데이터 가공 등...)
const login = (userInfo) => {
  console.log(userInfo);
  //   console.log(`email: ${userInfo.email} password: ${userInfo.password}`);
};
module.exports = login;
